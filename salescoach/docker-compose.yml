services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: salescoach
      POSTGRES_PASSWORD: salescoach
      POSTGRES_DB: salescoach
    volumes:
      - salescoach_pg:/var/lib/postgresql/data

volumes:
  salescoach_pg:
